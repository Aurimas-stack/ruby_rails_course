<h1>Categories list</h1>
<div>
    <%= will_paginate @categories, :container => false %>
</div>
<% @categories.each do |category| %>
    <h5><%= link_to category.name, category_path(category) %></h5>
    <%= pluralize(category.articles.count) %>
<% end %>