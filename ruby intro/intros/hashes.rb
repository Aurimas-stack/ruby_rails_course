# sample_hash = {'a' => 1, 'b' => 2, 'c' => 3}

# my_details = {'name' => 'real'}

# # my_details['name'] --> real
# # another_hash = {a: 1, b: 2, c: 3} --> in rails, --> {a:=>1, :b=>2, :c=>3}
# # another_hash[:a]
# # :a is a Symbol

# # sample_hash.keys
# # sample_hash.values

# # sample_hash.each do |key, value|
# # puts "The class for key is #{key.class}" and for value #{value.class}"
# # end

# # sample_hash['d'] = 5
# # another_hash[:e] = "string"
# # another_hash[:e] = "bla" -> :e = bla
# # another_hash.each { |some_key, some_value| puts "The key is #{some_key}, value #{some_value}"}

# # another_hash.select { |k, v| v.is_a?(String)} --> returns only strings
# # another_hash.each { |k, v| another_hash.delete(k) if v.is_a?(String)} --> deletes strings




